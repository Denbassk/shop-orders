# Стан проєкту на 14.09.2026 — що робити далі

## 1. Запис у сирі листи — ВИПРАВЛЕНО

14.09 у листі `_Сырые_Заказы_ВК` зникли замовлення 21 точки: `apiAppend_()`
писав із якорем `A1` і `insertDataOption: OVERWRITE`. Порожня комірка в
колонці A обривала «таблицю», і append писав поверх наявних рядків.

Зараз (коміт `b8ad9de`, `app/Orders.gs`):

```js
var anchor = Math.max(sh.getLastRow(), 1);
Sheets.Spreadsheets.Values.append(
  { values: rows }, spreadsheetId, "'" + sheetName + "'!A" + anchor,
  { valueInputOption: 'USER_ENTERED', insertDataOption: 'INSERT_ROWS' }
);
```

`INSERT_ROWS` вставляє нові рядки — затерти чуже не може за визначенням.
Якір на останньому заповненому рядку: розриви вище більше не збивають
визначення таблиці. `appendRows_` спільний, тож це діє на всі чотири
напрямки одразу.

Чорний фон, через який колись і взяли `OVERWRITE`, прибирають окремо:
`d08_fixRawFormat()` і `g07_repairBakeryRaw()`.

### Що по цьому лишилось

- `OVERWRITE` у коді більше немає ніде (лише в коментарях-поясненнях).
- `deleteTodayRows_()` видаляє рядки цілком (`deleteDimension`), порожніх
  комірок у колонці A не лишає. Архівування теж ріже рядки, а не вміст.
- **Слабке місце:** `archiveOneRebuild_()` (Archive.gs) робить
  `clearContent()` + `setValues()` під замком напрямку, а `apiAppend_`
  замок НЕ бере. Замовлення, що прийде саме під час перезбирання, може
  загубитись. Шлях рідкісний (ручний `d05_archiveRebuildHere`, або
  догін після `d01_archive` уночі), але варто закрити.
- Биті рядки з 4 значеннями замість 8 у `_Сырые_Заказы_ВК` і `_Архів`
  ще не почищені: `g04_cleanBakeryRaw()` і `g07_repairBakeryRaw()`.

---

## 2. Старі скрипти всередині таблиць

У кожної таблиці напрямку є свій Apps Script, прив'язаний до неї
(**Розширення → Apps Script**). Копії коду: `legacy/bakery`,
`legacy/bread`, `legacy/veg`, `registry`.

Генерація звітів перенесена в застосунок для всіх напрямків:

| Напрямок | Де тепер | Листи | Тригер |
|---|---|---|---|
| Випічка і кулінарія | `app/BakeryReport.gs` | Заказы ВК, Сводная ВК | `refreshBakeryReports` |
| Хліб ЧП Рома | `app/BreadReport.gs` | Заказы, Данные Заказов | `refreshBreadReports` |
| Овочі і фрукти | `app/VegReport.gs` | Замовлення Овочі, Зведена Овочі | `refreshVegReports` |
| Хліб НБХЗ | `app/Nbhz.gs` | Вивантаження ДД.ММ.РРРР | `e04_refreshExport` |

Спільна механіка: читається хвіст сирого листа за сьогодні, рахується
підпис стану (дата, рядки, ТТ, кількість), і звіт перезбирається лише
якщо підпис змінився. Список ТТ береться з Довідника ТТ з урахуванням
днів прийому, а не з окремих листів «Адреса ТТ» / «Маршруты и Магазины».

Лист «Заказы» для ЧП Рома лишився в тому самому вигляді: ті самі колонки,
рядок ЄДРПОУ + обліковий номер ТТ перед позиціями магазину.

## 2a. Зони доставки випічки (14.09, після інциденту)

Закупниця має бачити маршрут прямо в сирому листі, тому в колонці D
сирого листа випічки тепер **зона** (Салтовка / Новые дома / Центр /
не возится), а не категорія. Категорію звіти беруть з листа
«Ассортимент» по штрихкоду (`bakeryCatOf_`), тож групування у
«Сводной ВК» лишилось те саме.

- Джерело зони — **колонка S Довідника ТТ**. Заповнити разово:
  `g12_setupBakeryZones()`, перевірити — `g13_showBakeryZones()`.
- Новий лист **«Маршрути ВК»** — для машини розвозки: проста таблиця
  `Торгова точка | Найменування продукції | Кількість | Маршрут`,
  порядок маршрут → вид товару → адреса, після кожного товару рядок
  «Разом», після кожного маршруту — підсумок.
- Сирий лист сортується перед кожним перезбиранням звітів: спершу
  дата, далі назва товару (`sortBakeryRawByProduct_`). Сортування
  йде запитом `sortRange` до Sheets API, без читання-запису, тож
  замовлення, яке прийде в цю мить, не втрачається.
- Стара веб-форма в `legacy/bakery` писала в колонку D категорію.
  Після переходу нею користуватись не можна — тільки застосунок.

### Що треба зробити РУКАМИ після деплою

1. `clasp push` і нове розгортання застосунку.
2. `g05_fixBakeryRawSheet()` — шапка сирого листа на 8 колонок,
   у D тепер «Маршрут».
3. `g12_setupBakeryZones()` — зони в колонку S Довідника.
4. `d06_installTriggers()` — перестворює весь розклад, разом із трьома
   тригерами звітів.
5. Запустити руками і подивитись листи: `g01_bakeryReport()`,
   `h01_breadReport()`, `h03_vegReport()`.
6. У СТАРИХ проєктах зняти тригери, інакше два проєкти
   перезаписуватимуть одні й ті самі листи:
   - випічка (`legacy/bakery`) — `autoMaintenance`, кожні 5 хв;
   - овочі (`legacy/veg`) — `autoMaintenance` і `archiveOldOrders`;
   - хліб (`legacy/bread`) — `generateReportsAsync`,
     `rebuildReportsTriggered`.
   Самі проєкти не видаляти — у них лишається стара веб-форма.

---

## 3. Тимчасове, що вже застосовано

`app/Reopen.gs`:

- `z09_reopenBakeryToday()` — знімає дедлайн на день і чистить позначки
  `sub_<напрямок>_*`. Дублів не створює: у кого рядки цілі, тих відсікає
  перевірка листа.
- `z13_closeBakeryToday()` — повернути звичайний режим.
- `z14_clearMarksForList()` — зняти позначки за списком адрес.

`app/DiagToday.gs`:

- `z01_checkBakeryToday()` та аналоги — хвіст листа, типи дат, скільки
  рядків за сьогодні, скільки позицій бачить застосунок, стан днів тижня.

---

## 4. Дрібне, не зроблено

- Мост до файлів після оновлення Windows від 08.09 не монтує папку в
  робочу оболонку. Обхід є: Claude працює з `D:\shop-orders` через
  desktop-commander, git запускається звідти ж.
- Асортимент випічки: з 24 позицій доступні 4, решта на «стоп». Це
  навмисно, але прямо пояснює малу кількість замовлень.
- Замок напрямку і `apiAppend_` живуть окремо — див. слабке місце в п.1.
